{
  "routes": [
    {
      "method": "GET",
      "path": "/ranks",
      "handler": "rank.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/ranks/count",
      "handler": "rank.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/ranks/:id",
      "handler": "rank.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/ranks",
      "handler": "rank.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/ranks/:id",
      "handler": "rank.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/ranks/:id",
      "handler": "rank.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
