{
  "routes": [
    {
      "method": "GET",
      "path": "/profile",
      "handler": "profile.index",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/profile",
      "handler": "profile.update",
      "config": {
        "policies": []
      }
    }
  ]
}